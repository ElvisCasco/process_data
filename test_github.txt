# Navigate to your project folder
Set-Location "c:\EC\BSE\DSDM\Term 1\21DM004 Computing for Data Science\hw4\process_data"

# Initialize git repository
git init

# Create .gitignore file
@"
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/
ENV/
.ipynb_checkpoints/
.DS_Store
"@ | Out-File -FilePath .gitignore -Encoding utf8

# Add all files
git add .

# Commit
git commit -m "Initial commit: process_data package"

# Create GitHub repo (you need GitHub CLI installed, or do this manually on github.com)
# Manual way: Go to github.com → New Repository → Name it "process_data"

# Link to your GitHub repo (replace YOUR_USERNAME)
git remote add origin https://github.com/ElvisCasco/process_data.git

# Pull the remote changes first
git pull origin main --allow-unrelated-histories

# If there are conflicts, resolve them, then:
git add .
git commit -m "Merge remote changes"

# Now push
git push -u origin main